#ifndef DEFINITIONS_H
#define DEFINITIONS_H

#include <string>

namespace constants
{
    const std::string counterexample("Counterexample:");
    const std::string line("line");
    const std::string trace_delimiter("----------------------------------------------------");
    const std::string verification_failed("VERIFICATION FAILED");
    const std::string verification_successful("VERIFICATION SUCCESSFUL");
}

#endif
